services:
  - type: cron
    name: gojo-shop-check
    env: python
    plan: free
    schedule: "0 15 * * *"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python check_shop.py"
